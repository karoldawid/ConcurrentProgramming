<Window x:Class="Presentation.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:Presentation.ModelView"
        Title="Ball Simulation"
        Height="600" Width="800"
        Background="White"
        SizeToContent="WidthAndHeight">

    <Window.DataContext>
        <local:ModelView />
    </Window.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0" HorizontalAlignment="Center" Margin="10">
            <TextBlock Text="Ball count:" VerticalAlignment="Center" Margin="5,0"/>
            <TextBox Name="AmountTextBox" Width="50" Height="25" Text="{Binding Amount}" Margin="5,0"/>
            <Button Content="Start" Width="70" Height="25" Command="{Binding StartCommand}" Margin="5,0"/>
            <Button Content="Stop" Width="70" Height="25" Command="{Binding StopCommand}" Margin="5,0"/>
            <Button Content="Add" Width="70" Height="25" Command="{Binding SummonCommand}" Margin="5,0"/>
            <Button Content="Clear" Width="70" Height="25" Command="{Binding ClearCommand}" Margin="5,0"/>
        </StackPanel>

        <Border Grid.Row="1" Margin="20" BorderBrush="Green" BorderThickness="1"
                Width="700" Height="500" HorizontalAlignment="Center" VerticalAlignment="Top">
            <ItemsControl ItemsSource="{Binding Balls}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <Canvas Background="White">
                            <Canvas.Effect>
                                <DropShadowEffect BlurRadius="10" ShadowDepth="0" Color="#FFD3D3D3"/>
                            </Canvas.Effect>
                        </Canvas>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemContainerStyle>
                    <Style TargetType="ContentPresenter">
                        <Setter Property="Canvas.Left" Value="{Binding X}" />
                        <Setter Property="Canvas.Top" Value="{Binding Y}" />
                    </Style>
                </ItemsControl.ItemContainerStyle>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Path Fill="Blue" Stroke="DarkBlue">
                            <Path.Data>
                                <EllipseGeometry RadiusX="{Binding Radius}" RadiusY="{Binding Radius}"/>
                            </Path.Data>
                        </Path>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Border>
    </Grid>
</Window>